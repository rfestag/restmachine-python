Move Jinja-based content handling to a separate package (restmachine-jinja)

Investigate security warnings that are probably surpressed

Set main to protected, start using branching/MRs for releases. 

Review all tests to ensure they match what I would expect

Add websocket support

Identify opportunities for improved S3 support

DSL apparently doesn't accept query strings?

Add support for ProxyV2

Add hotswap support

Add support for ASGI extensions (Zero copy send, Http/2 server push)
