"""
Health check endpoints for {{ project_name }}.
"""

from restmachine import Router

router = Router()


@router.get('/')
def health_check():
    """
    Health check endpoint.

    Returns a simple status message to verify the application is running.
    """
    return {
        'status': 'ok',
        'service': '{{ project_name }}'
    }
