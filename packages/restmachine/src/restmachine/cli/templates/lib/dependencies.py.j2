"""
Application-wide dependencies for {{ project_name }}.

Define dependencies here that can be injected into route handlers.

Example:
    def database():
        '''Provide database backend for dependency injection.'''
        from models import backend
        return backend
"""
