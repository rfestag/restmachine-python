"""
{{ project_name }} models.

Define your models here using restmachine-orm.

Example:
    from restmachine_orm import Model, field

    class User(Model):
        id: str = field(primary_key=True)
        name: str
        email: str

For backend setup, see lib/dependencies.py.
"""
